{
  "name": "eslint-config",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/eslint-config",
  "projectType": "library",
  "tags": ["type:tooling", "scope:shared", "platform:neutral"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/eslint-config/**/*.mjs"
      }
    }
  }
}
