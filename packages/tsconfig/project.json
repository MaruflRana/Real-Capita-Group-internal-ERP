{
  "name": "tsconfig",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/tsconfig",
  "projectType": "library",
  "tags": ["type:tooling", "scope:shared", "platform:neutral"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --check packages/tsconfig/**/*.json"
      }
    }
  }
}
